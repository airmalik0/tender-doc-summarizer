# Зависимости рантайма с зафиксированными версиями — на них собирается образ
# и гоняется CI. Источник правды по метаданным пакета — pyproject.toml;
# здесь версии закреплены ради воспроизводимости сборки.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.15.0

# Разбор PDF: текстовый слой и рендер страниц под OCR
pypdfium2==5.13.0
pytesseract==0.3.13
pillow==12.3.0

# LLM-провайдеры. Ни один ключ не обязателен: без ключей сервис работает
# на детерминированных правилах.
anthropic==1.0.0
openai==3.3.1
google-genai==2.19.0
